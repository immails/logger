{
  "name": "@_immails/logger",
  "version": "1.0.22",
  "description": "ESM/TS Logger package for my private projects. This package is not intended for public use, but uh...",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    "import": "./dist/*.js",
    "default": "./dist/*.js"
  },
  "files": [
    "dist/",
    "package.json"
  ],
  "scripts": {
    "prepublish": "npm version patch && tsc",
    "update": "git commit -a -m update && git push && npm version patch && tsc && npm publish --access public"
  },
  "author": "immails",
  "license": "ISC"
}
